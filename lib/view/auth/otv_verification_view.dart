import 'package:flutter/material.dart';

class OtvVerificationView extends StatelessWidget {
  const OtvVerificationView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
